{
  "manifest_version": 3,
  "name": "UCLA Dining",
  "description": "Add some extensions (Firebase later)",
  "version": "1.0",
  "content_scripts": [{
    "matches": ["<all_urls>"],
    "js": ["App.js"]
  }],
  "permissions": [
    "tabs",
    "activeTab"
  ]
}
